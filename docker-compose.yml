version: "3.9"

services:
  ai-firewall-mcp:
    build: .
    image: ai-firewall-mcp:latest
    container_name: ai-firewall-mcp
    environment:
      - FIREWALL_MODE=strict
      - LOG_LEVEL=INFO
    stdin_open: true
    tty: true
